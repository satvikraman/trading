# Minimal pip deps for local venv (Telegram client + tests).
# Full trading stack on Windows still uses conda (see requirements.txt).
requests>=2.28.0
python-dotenv>=1.0.0
pytest>=7.0.0
selenium>=4.0,<5
webdriver-manager>=4.0,<5
flask>=2.2,<4
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
